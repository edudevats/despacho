# Core Flask Framework
Flask==3.1.3
# Werkzeug 3.0.3 has CVE-2024-49767 (multipart DoS) and CVE-2024-49766 (Windows safe_join traversal).
Werkzeug>=3.1.8,<3.2

# Flask Extensions
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.1.0
Flask-Login==0.6.3
Flask-WTF==1.3.0
Flask-Mail==0.10.0
Flask-Caching==2.4.0
# Rate limiting — desactivado temporalmente (stub no-op en extensions.py).
# Descomenta e instala cuando Flask-Limiter esté disponible en el entorno.
# Flask-Limiter==4.1.1

# Forms and Validation
WTForms==3.2.2
email-validator==2.3.0

# Database
SQLAlchemy==2.0.49
Alembic==1.18.4

# Security and Authentication  
passlib[bcrypt]==1.7.4
bcrypt==5.0.0
itsdangerous==2.2.0

# Image Processing
# Pillow 10.4.0 has CVE-2025-48433 (DoS via crafted ICO/TIFF). Upgrade required.
Pillow>=12.2.0
qrcode[pil]==8.2

# Configuration and Environment
python-dotenv==1.2.2

# CLI and Development
click==8.3.3

# Production Server
gunicorn==26.0.0

# Development Tools
pytest==9.0.3
pytest-flask==1.3.0
coverage==7.14.0

# Performance Monitoring  
psutil==7.2.2

# Timezone handling
pytz==2026.2
backports.zoneinfo==0.2.1;python_version<"3.9"  # For Python < 3.9 timezone support

# Additional utilities
# requests 2.32.3 has CVE-2024-47081 (.netrc credential leak).
requests>=2.34.0
# Jinja2 3.1.4 has CVE-2024-56326 + CVE-2025-27516 (sandbox bypass).
Jinja2>=3.1.6
MarkupSafe==3.0.3

# Cryptography (used for Fernet encryption of PAC creds — must stay current).
# cryptography 43.0.1 has CVE-2024-12797 (bundled OpenSSL RFC 7250 issue).
cryptography>=48.0.0
PyJWT==2.12.1

# Python compatibility
setuptools>=82.0.1

# SAT CFDI
satcfdi==4.9.17

# PDF Generation (satcfdi uses weasyprint/reportlab internally)
weasyprint==68.1
pydyf>=0.12.1,<0.13
reportlab==4.5.0

# XML Processing
lxml==6.1.0
# Defensive XML parsing: rejects DOCTYPE/entity expansion before lxml/satcfdi see it.
defusedxml>=0.7.1

# Date utilities
python-dateutil==2.9.0.post0